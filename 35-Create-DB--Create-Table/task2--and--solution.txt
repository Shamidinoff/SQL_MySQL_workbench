1. Удалите ранее созданную БД test  (DROP DATABASE) .
2. Откройте сохраненный в предыдущем задании SQL-файл DDL1.
3. Обратите внимание на столбец EmplDate. Эта дата по умолчанию стоит 2010-08-12, но сделайте так, чтобы нельзя было поставить более раннюю дату (CHECK).

Запустите SQL-скрипт.

Подсказка:

EmplDate DATE DEFAULT "2010-08-12" CHECK(EmplDate >='2010-08-12')

4. Проверьте как работают установленные Вами в этом и предыдущих заданиях ограничения. Для этого: 
- выгрузите таблицу person (SELECT * FROM test.person)
- внесите в выгрузку строку с такими данными: ID - 1, Name - любой набор букв, EmplDate - 2010-06-01 (дата, которая раньше создания компании!!!), Birth - любая дата (например, 2000-09-01),  Doc - любой набор допустим с 10 цифр, Salary - поставьте отрицательное значение, например, -100000.
- нажмите кнопку APPLY - и в появившемся окне попытайтесь в завершить внесение данных в БД. Вы получите ошибки проверки данных: сначала на зарплату\salary. А если Вы сделаете зарплату неотрицательной (100000)  - то снова не пройдет, потому что будет нарушено правило на дату приема (EmplDate), и только если его Вы смените на любую дату начиная с 2010-08-12 - то данные внесутся в таблицу. 